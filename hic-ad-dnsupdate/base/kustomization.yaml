apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - dnsupdate-job.yaml

configMapGenerator:
  - name: dnsupdate-job-scripts
    namespace: ad
    files:
      - dnsupdate-job.sh
